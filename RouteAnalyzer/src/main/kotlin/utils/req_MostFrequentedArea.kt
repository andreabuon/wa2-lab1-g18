package org.example.util

import org.example.model.Waypoint

fun findMostFrequentedArea(waypointList: List<Waypoint>, areaRadiusKm: Double): Pair<Waypoint, Int> {
    // TODO: calculate all distances first in a matrix and then for each waypoint
    //  calculate how many are closer than areaRadiusKm; at last find the most frequented one
    TODO()
}
